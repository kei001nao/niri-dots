{
	// 読み込み中にソースとして使用する別の設定ファイルを指すパスのリスト。
	// メインの設定ファイルで定義された値は、インポートされたファイルよりも常に優先されることに注意してください。
	// 相対パスが受け入れられ、インポート元の設定ファイルが配置されている設定ディレクトリからの相対パスになります。
	"imports": [],

	// ルート検索でファイルを直接検索するかどうか。
	// ファイル検索は非同期で実行されます。クエリが入力されてからファイルが表示されるまでに少し時間がかかる場合があります。
	// これをオンにすると、検索時のCPU使用率が増加します。
	// vicinaeには、豊富なコンテンツプレビューを伴うファイル検索を単独で実行するために使用できる、組み込みのファイル検索コマンドがあることに注意してください。これはほとんどのユーザーにとって十分すぎるはずです。
	"search_files_in_root": false,

	// 「escape」キーが押されたときにvicinaeが何をすべきか
	// 「navigate_back」または「close_window」をサポート
	"escape_key_behavior": "navigate_back",

	// レイヤーシェルプロトコルを使用してウィンドウを配置し、キーボードの対話性が「exclusive」（デフォルト）に設定されている場合、この設定は効果がありません。 「on_demand」対話性に切り替えるか、レイヤーシェルを無効にしてください。
	"close_on_focus_loss": false,

	// IMEのプリエディット文字列をリアルタイムで検索するかどうか。
	"consider_preedit": false,

	// ウィンドウが閉じるたびにナビゲーション状態をリセットする
	"pop_to_root_on_close": false,

	// ファビコンの読み込みが必要な場合に使用するファビコンサービス。
	// 利用可能な値は次のとおりです： 'twenty' | 'google' | 'none'
	// これが 'none' に設定されている場合、ファビコンの読み込みは無効になり、ファビコンが期待される場所でプレースホルダーアイコンが使用されます。
	"favicon_service": "twenty",

	// 実験的！
	// メインの検索バーとナビゲーション中に特定​​の編集モーションを有効にします。
	// これは将来のアップデートで変更される可能性があります。
	// サポート： 'default' | 'emacs'
	"keybinding": "default",

	"font": {
		// 一般的なvicinae UIに使用するフォントファミリ。
		"normal": {
			"family": "auto",
			// フォントのポイントサイズ（ピクセルサイズとは異なり、ポイントサイズは解像度に応じて拡大縮小されます）
			"size": 10.5
		}
	},

	// 一般的なvicinaeテーマとシステムアイコンテーマ（アプリケーションとファイルアイコンに使用）は、システムの外観に応じてカスタマイズできます。
	// vicinae GUIを介して現在のテーマを編集すると、現在のシステムの外観にマッピングされる値が変更されます。
	"theme": {
		"light": {
			"name": "vicinae-light",
			"icon_theme": "auto"
		},
		"dark": {
			"name": "vicinae-dark",
			"icon_theme": "auto"
		}
	},

	"launcher_window": {
		// メインウィンドウの不透明度を制御します。
		// 他のサーフェスの不透明度は、アクティブなテーマによって制御されます。
		"opacity": 0.95,
		
		// ウィンドウの背景をぼかします。
		// 現時点ではHyprlandでのみサポートされています。
		// 適切に無効にするには、サーバーの再起動が必要になる場合があります。
		"blur": {
			"enabled": true
		},

		// vicinaeウィンドウの背後にあるすべてを暗くします。
		// 現時点ではHyprlandでのみサポートされています。
		// 適切に無効にするには、サーバーの再起動が必要になる場合があります。
		"dim_around": true,

		// コンポジターに丸みを帯びた境界線などを描画させたい場合は、クライアント側の装飾をオフにすることができます。これにより、通常はより良い結果が得られますが、可能であればセットアップがより複雑になります。
		"client_side_decorations": {
			"enabled": true,
			"rounding": 10,
			"border_width": 3
		},

		// コンパクトモードでは、vicinaeはクエリが入力されていない場合、ルート検索に検索バーのみを表示し、検索時にのみフルサイズに展開します。
		// 警告：コンパクトモードは、通常のフローティングウィンドウとは対照的に、vicinaeがレイヤーサーフェスとしてレンダリングされる場合に最適に機能します（利用可能な場合はデフォルト）。
		// これは、コンパクトモードでレンダリングされない部分がまだフルウィンドウサイズの一部であるため、多くの視覚的ノイズを発生させる可能性のあるコンポジターウィンドウのサイズ変更に対処することなく、ウィンドウを正常に拡張できるためです。サーバー側の境界線とぼかしは、著しく場違いに見えます。
		"compact_mode": {
			"enabled": false
		},

		// vicinae UIは、デフォルトのサイズで最適に動作するように設計されています。これを変更する場合は、同様のアスペクト比を維持することを強くお勧めします。
		"size": {
			"width": 770,
			"height": 480
		},

		// 実験的 - X11のみ！
		// vicinaeウィンドウを表示する必要がある画面の名前（xrandrなどのツールによって提供される）。
		// 例：「eDP1」、「DP-1」
		"screen": "auto",

		// 「wlr-layer-shell」プロトコルをサポートするwaylandコンポジターのみ（https://wayland.app/protocols/wlr-layer-shell-unstable-v1）
		// 注：cosmicコンポジターのレイヤーシェルサポートは、現在壊れているため、明示的に無効になっています
		"layer_shell": {
			"enabled": true,
			"keyboard_interactivity": "exclusive", // 'exclusive' | 'on_demand'
			// 'overlay'または'top'のいずれか。
			// 他のレイヤーは、ランチャーには適していないためサポートされていません。
			// 「overlay」を使用すると、一部のシナリオでIMEポップオーバーの上にvicinaeウィンドウが表示されるため、「top」をお勧めします。
			"layer": "top" 
		}
	},

	// キーバインドはカスタム形式を使用してシリアル化されます。
	// 設定GUIを介して編集することをお勧めします。これにより、このファイルに書き込まれます。
	"keybinds": {
		// 一般的なショートカット
		"open-search-filter": "control+P",
		"open-settings": "control+,",
		"toggle-action-panel": "control+B",

		// 汎用アクションにショートカットを割り当てるために使用されます
		// 拡張機能が特定の種類のアクションを提供する場合、ここで定義されているショートカットを使用することをお勧めします。
		"action.copy": "control+shift+C",
		"action.copy-name": "control+shift+.",
		"action.copy-path": "control+shift+,",
		"action.dangerous-remove": "control+shift+X",
		"action.duplicate": "control+D",
		"action.edit": "control+E",
		"action.edit-secondary": "control+shift+E",
		"action.move-down": "control+shift+ARROWDOWN",
		"action.move-up": "control+shift+ARROWUP",
		"action.new": "control+N",
		"action.open": "control+O",
		"action.pin": "control+shift+P",
		"action.refresh": "control+R",
		"action.remove": "control+X",
		"action.save": "control+S"
	},

	// 「お気に入り」としてタグ付けされたエントリポイントのリスト。
	// 検索クエリがアクティブでない場合、ルート検索の最上部に表示されます。
	// 各値は、シリアル化されたエントリポイントIDです。
	"favorites": [
		"clipboard:history"
	],

	// 提供された検索クエリに一致する結果がない場合にフォールバックとして提案するエントリポイントのリスト。
	// 各値は、シリアル化されたエントリポイントIDです。
	"fallbacks": [
		"files:search"
	],

	// vicinaeルート検索からアクティブ化できるすべてのアイテムは、エントリポイント、または場合によっては拡張機能の場合、「コマンド」と呼ばれます。
	// エントリポイントは常にプロバイダーの下にグループ化されます。
	// 例：アプリケーションのエントリポイントはすべて「applications」プロバイダーの下にグループ化されます
	//
	// プロバイダーとエントリポイントを操作する最良の方法は、設定ウィンドウ（「拡張機能」タブ）によって提供されるグラフィカルインターフェイスを使用することです。このGUIを介して変更が行われるたびに、関連する値が以下の「providers」オブジェクトに書き込まれます（パスワード設定は除く）。
	//
	// 利用可能なプロバイダーの正確なリストは、インストールされている拡張機能によって異なります。
	"providers": {}
}
